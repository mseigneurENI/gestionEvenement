<nav>
    <ul>
        {% if app.user %}
        <li><a href="{{ path('main_home') }}">Home</a></li>
        <li><a href="{{ path('events_list') }}">Liste des sorties</a></li>
        <li><a href="{{ path('events_create') }}">Créer une sortie</a></li>
        <li><a href="{{ path('user_update') }}">Voir mon profil</a></li>
        <li><a href="{{ path('app_logout') }}">Logout</a></li>
        {% else %}
            <li><a href="{{ path('app_login') }}">Login</a></li>
        {% endif %}
    </ul>
</nav>

{% extends 'base.html.twig' %}

{% block body %}
    <h1>Créer un campus</h1>
    {{ form_start(form) }}
    {{ form_widget(form) }}
    <button>Ajouter</button>
    {{ form_end(form) }}
{% endblock %}
